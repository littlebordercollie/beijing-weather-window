# 雨窗验收记录

验收日期：2026-07-11（Asia/Shanghai）

## 需求逐项核对

| 用户要求 | 实现 | 验收证据 |
|---|---|---|
| 公网网址并部署到 GitHub | GitHub Pages，公开仓库，HTTPS | 2026-07-11 快照：`https://littlebordercollie.github.io/beijing-weather-window/` 返回 HTTP 200；Pages API 显示 `public=true`、`https_enforced=true` |
| 有背景、有完整视觉 | 深色城市气象值班室视觉、地图底图、气象圆环与雨线背景 | 本地生产页面在 1280×720 和 390×844 浏览器中实际渲染检查 |
| 输入地点 | 常用北京地点本地可信索引、Photon 尽力而为的在线搜索、经纬度输入、地图点选、设备定位 | 单元测试以 mock 覆盖本地点、经纬度、在线结果筛选；真实跨域搜索、定位权限和地图点选仍属于浏览器/第三方集成路径，不在单元测试证明范围内 |
| 设置多久到达 | 15—180 分钟，15 分钟步进 | 浏览器 DOM 显示滑块；确定性分析测试覆盖 ETA 时次匹配 |
| 设置 3/5 公里 | 两档半径并同步到 URL 和两个页面 | 浏览器实点 5 km 后 `aria-pressed=true`、URL 为 `radius=5` |
| 地图页面 | 目的地、半径圈、参考台站、四个周边探针、深色底图 | 本地浏览器实际加载 CARTO/OSM 瓦片并显示朝阳站 54433 |
| 列表页面 | 站点卡、时间线、五点探针、官方雨情页入口、来源账本 | 本地浏览器 DOM 和完整页面截图检查；公网 `list.html` 返回 HTTP 200 |
| 附近有多少气象观测站 | 从 18 个官方公开参考台站坐标计算 3/5 km 范围 | 默认朝阳公园 3 km 内返回 1 个：朝阳 54433，距离约 2.8 km；距离算法测试覆盖 |
| 每站最后时次、更新信息、时段终点 | 显示站旁格点时次、温度/雨量/气压/湿度、当前值时段终点和下次页面检查 | 这些是模型格点产品时间，不是台站原始上传或数据提供方的下次更新时间 |
| 推测接下来发生什么 | ETA 风险、未来两小时降雨时间线、干燥窗口、趋势、五点空间差异 | 分析模块为确定性程序；风险、干燥窗口、空间证据不足不推断等测试覆盖 |
| 地图与列表状态一致 | 地点、坐标、ETA、半径写入 URL 与 localStorage | 直接打开带参数的列表页后显示同一朝阳目的地、45 分钟、5 km 和 1 个站 |
| 手机可用 | 720px 以下单列布局、时间线容器内横向滚动 | 390×844 浏览器验收：`scrollWidth=390`，无横向溢出 |

## 数据边界

- 18 个台站的站号、坐标和海拔来自北京市地方标准 DB11/T 1643—2023。
- 卡片实时数值是 Open-Meteo 站旁格点，不是台站原始观测；界面逐卡标注。
- 北京市水务数据的公开再使用权未确认；当前版本仅链接官方页，不调用其数据接口。
- 五点探针落入不足 3 个独立模型网格时，程序拒绝推断雨带方向。
- 页面是出行辅助，不是官方气象预报；强对流天气以官方预警为准。

## 自动化与构建

当前确定性单元测试覆盖分析、距离/探针、本地点索引、坐标输入、URL 状态校验和自动刷新故障恢复。它们不覆盖第三方 SLA、接口 schema 长期稳定性、浏览器定位权限、地图点选或跨浏览器行为。

```text
Test Files  5 passed (5)
Tests       23 passed (23)
vite build  success
```

Pages 当前仍从 `main:/docs` 发布。本次发布在本地使用锁定依赖完成：

```text
npm ci
  -> npm test
  -> npm run build
  -> 提交 docs/
  -> legacy Pages 发布 main:/docs
```

上述是本次发布证据，不是远端强制门禁。当前 GitHub OAuth 凭据没有 `workflow` 权限，因此无法在本次提交中启用 Actions Pages 工作流；获得该权限后仍应将构建与测试改为远端强制门禁。

生产入口与公网文件字节一致：

```text
index.html  908798580e72d6020ea68fb0c9ecbaaca74b2d74643e81bc36aaa17e297007b0
map js      4cee1e6c34687e589467eef82a83ea2fb4fd5b538675459fc690c7fa8a62d5b7
list.html   baf15fb5491219f031407bf7804d1019360bf31198a455766e7e832c31a2b4b5
```

以上三个哈希是修复前的 2026-07-11 快照：当时由本地 `docs/` 文件与 GitHub Pages 下载文件分别计算，结果逐一相同。本次修复发布后将以新哈希覆盖该记录。

## 浏览器核验说明

本地生产页面完成了桌面、手机、地图、列表、半径切换和一次实时数据渲染检查。Codex 内置浏览器直接导航 GitHub Pages 时遇到 `ERR_CONNECTION_RESET`，因此没有把公网浏览器访问写成成功。公网部署证据限于当时的 Pages 构建状态、HTTPS 200、首页/列表/脚本资源响应，以及公网文件与本地生产快照的逐字节哈希一致。

这些证据不等同于跨浏览器、跨网络或第三方故障条件下的端到端验收。第三方许可和数据使用边界见 `THIRD_PARTY_NOTICES.md`。
